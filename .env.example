# WebRTC signaling server URL.
# For local dev: ws://localhost:4444 (started automatically via `npm run dev`)
# For production: wss://your-deployed-signaling-server.com
NEXT_PUBLIC_SIGNALING_URL=ws://localhost:4444
